= simple_form_for(@taxon) do |f|
  = f.error_notification

  .inputs
    = f.association :parent
    = f.association :taxonomy
    = f.input :name

  .actions
    = f.button :submit, :class => "btn primary"
